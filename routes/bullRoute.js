// const express = require("express");
// const router = express.Router();
// const { registerUser } = require("../controller/bullController");

// router.post("/register", registerUser);
// // router.get("/", bullController.getUsers);
// // router.get("/:id", bullController.getUser);
// // router.put("/:id", bullController.updateUser);
// // router.delete("/:id", bullController.deleteUser);
// module.exports = router;
